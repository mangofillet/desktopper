# Drop paper PDF files here to publish them.
# In edit mode, upload a PDF (works instantly in your browser via IndexedDB),
# then also copy the same file into this folder and commit it so every visitor
# sees the real PDF. portfolio.json references them as /papers/<filename>.
